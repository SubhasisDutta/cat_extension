# Android Studio / Gradle
*.iml
.gradle/
.idea/
build/
local.properties
captures/
.cxx/

# Generated wrapper jar (run `gradle wrapper` to regenerate)
gradle/wrapper/gradle-wrapper.jar

# Keystores and signing material must never be committed.
# `keystore.properties.template` IS committed; the real one is not.
*.jks
*.keystore
keystore.properties
keystores/
release/

# AAB / APK release artifacts (regenerated on demand by scripts/build-release.sh)
dist/
*.aab
*.apk

# OS
.DS_Store
